# 工具
## Linux 下对拍
```bash
make gen && make test && make std
while true; do
    ./gen > data.in
    ./test < data.in > test.out
    ./std < data.in > std.out
    if diff -w test.out std.out; then
        printf "AC\n"
    else
        printf "WA\n"
        exit 0
    fi
done
```